import axios from "axios";
import {
    Card,
    CardContent,
    Typography,
    Grid,
    Box,
    Container,
} from "@mui/material";
import LoginForm from "@/components/auth/LoginForm";
const LoginPage = () => {

    return (
        <LoginForm />
    );
};

export default LoginPage;
